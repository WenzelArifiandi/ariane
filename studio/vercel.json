{
  "ignoreCommand": "# Studio: only build on main, skip PRs; rely on Deploy Hook\nif [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi;\n# Build only if files in this directory changed\ngit diff --quiet HEAD^ HEAD -- . 2>/dev/null && exit 0 || exit 1"
}
