import project from '../schemas/project'
import link from '../schemas/link'
import about from '../schemas/about'
import approvedUser from '../schemas/approvedUser'
import accessRequest from '../schemas/accessRequest'

export const schemaTypes = [project, link, about, approvedUser, accessRequest]
