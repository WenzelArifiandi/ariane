---
# Cell v0 Encrypted Secrets
# This file contains sensitive configuration values for the Cell v0 deployment
# Encrypt with: sops -e -i secrets/cell-v0.yml

# PostgreSQL Configuration
postgres_password: "CHANGEME_SECURE_POSTGRES_PASSWORD"
postgres_replication_password: "CHANGEME_SECURE_REPLICATION_PASSWORD"
postgres_exporter_password: "CHANGEME_SECURE_EXPORTER_PASSWORD"

# Zitadel Configuration
zitadel_db_password: "CHANGEME_SECURE_ZITADEL_DB_PASSWORD"
zitadel_master_key: "CHANGEME_SECURE_ZITADEL_MASTER_KEY_32_BYTES"

# Cloudflare API for cert-manager
cloudflare_api_token: "CHANGEME_CLOUDFLARE_API_TOKEN_FOR_DNS"

# Monitoring Configuration
grafana_admin_password: "CHANGEME_SECURE_GRAFANA_PASSWORD"
prometheus_basic_auth_password: "CHANGEME_SECURE_PROMETHEUS_PASSWORD"

# Backup Configuration
pgbackrest_s3_key: "CHANGEME_S3_ACCESS_KEY"
pgbackrest_s3_key_secret: "CHANGEME_S3_SECRET_KEY"
pgbackrest_s3_bucket: "ariane-postgres-backups"
pgbackrest_s3_region: "us-east-1"
pgbackrest_s3_endpoint: "s3.amazonaws.com"

# Email Configuration
acme_email: "ops@wenzelarifiandi.com"
smtp_password: "CHANGEME_SMTP_PASSWORD"

# SSH Keys (if needed for automation)
deployment_ssh_private_key: |
  -----BEGIN OPENSSH PRIVATE KEY-----
  CHANGEME_SSH_PRIVATE_KEY_CONTENT
  -----END OPENSSH PRIVATE KEY-----